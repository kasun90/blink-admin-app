import { AdminUserTypePipe } from './admin-user-type.pipe';

describe('AdminUserTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminUserTypePipe();
    expect(pipe).toBeTruthy();
  });
});
